# USER.md - About Your Human

## Current User (佛罗多老爷 / Frodo)

- **Name:** 佛罗多老爷 (Frodo)
- **What to call them:** 佛罗多老爷 or just Frodo
- **Language:** 中文
- **Timezone:** Asia/Shanghai (GMT+8)

### Context

- Has a **$50,000 USD loan** to repay in ~4 months (no clear plan yet)
- Currently **no clear income/skill source** mentioned
- Very **practical**, focused on action over emotion
- Wants me to "think more, practice more" — not just talk
- Stopped loan discussion temporarily (2026-05-28)
- First contact: 2026-05-28, ~22:37 GMT+8 via QQ

### Open Questions

- Exact location?
- Current occupation?
- Loan details (source, interest rate, purpose)?
- What did Frodo mean by "pre-added some skills"?

---

## 📋 Onboarding Template (新会话第 1 步必填)

> 复制以下 YAML 块，填好后覆盖本节。可跳过/默认，但**推荐全部填**。

```yaml
# === USER.md Onboarding 必填项 ===
# 说明: 这是 80% portable 复现所需的用户上下文。填完后可让新会话不只“接手人设”还能“接手工作风格”。

# 1. 身份
name: <您的称呼 (例: 佛罗多老爷)>
language: <中文 | English | mixed>
timezone: <Asia/Shanghai or IANA tz>

# 2. 工作风格 (选一)
work_style: <think_more_practice_more | one_shot | iterate | collaborate>
# think_more_practice_more = 多想多练, 质量优先
# one_shot = 一气吪成, 快进快出
# iterate = 反复打磨, 迭代改进
# collaborate = 联动多轮, 实时反馈

# 3. 字段内规 (Apple 官方 / 自定义)
field_limits:
  promotional_text: <170 | 100 | custom:90>   # Apple 上限 170
  description: <4000 | custom:2000>          # Apple 上限 4000
  keywords: <100 | 80 | custom:60>            # Apple 上限 100 bytes
  copyright: <YYYY_OwnerName | with_(c)>     # 末尾是否加 ©

# 4. 节奏
daily_report_time: <08:00 | custom:09:30>     # 早报时间
self_check_script: <~/.openclaw/workspace/scripts/sop-822-check.sh>

# 5. 阻塞 vs 非阻塞 (重要!)
block_threshold:
  app_store_reject: always_block              # 任何审核拒绝都阻塞
  private_data_leak: always_block             # PII 泄漏都阻塞
  workspace_pollution: block_if_recurring    # 根目录污染不反复出现才阻塞
  url_404: warn_only                          # URL 404 警告而不阻塞

# 6. 沟通偏好
comm_style: <concise_only | detailed_with_examples | show_code_blocks>
# concise_only = 报告型, 不多解释
# detailed_with_examples = 举例型
# show_code_blocks = 重代码, 轻描述

# 7. 项目上下文
current_app: <VitaMindGo | other>
ios_team_id: <9L6N2ZF26B | your_team_id>
github_user: <lauer3912 | your_username>
bundle_id_prefix: <com.ggsheng. | your.prefix.>
```

### 读取顺序

新会话开始时, Agent 应:
1. 读本节 (onboarding)
2. 读 `AGENTS.md` (人设/工作协议)
3. 读 `SOUL.md` (灵魂/语气)
4. 读 `MEMORY.md` (跨项目长期记忆)
5. 读 `IDENTITY.md` (我是谁)
6. 读 `docs/SOP-iOS-Local-Development.md` §0 (环境 onboarding)

### 字段详说

**work_style** 影响 Agent 决策:
- `think_more_practice_more` → 每次任务先 1-2 句思考, 再动手, 最后验证
- `one_shot` → 直接动手, 少前戏
- `iterate` → 先出个 v0, 跑反馈, 修 1 轮, 走人
- `collaborate` → 每步报你, 你选个分支

**field_limits** 影响 Listing.md 内部限制检查 (不是 Apple 限制, 是你定的更严标准)

**block_threshold** 影响 SOP 错误处理:
- `always_block` = 出现即等修后才能走
- `block_if_recurring` = 第二次出现才等修
- `warn_only` = 仅报告, 不限行动

**comm_style** 影响报告长度:
- `concise_only` = 1-2 行, 0 表情
- `detailed_with_examples` = 报告 + 例子
- `show_code_blocks` = 重代码轻文字