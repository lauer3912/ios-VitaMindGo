# 2026-06-15 运维日志

## 22:00-23:00 — 根本问题1修复: agent-bus cron 认证故障

### 问题
Katherine-yl2rKS 22:19/22:40 报告3问题, 其中根本问题1: **cron 跑但 gh 认证失败**。

我在22:47回复 "cron装好了" 但实际: cron SHELL跑, gh认证失败, 所有 poll/watch 全空跑。

### 根因 (双叠加)
1. **cron PATH 问题** (22:47已修): cron PATH=/usr/bin:/bin, 不含 /opt/homebrew/bin, gh 找不到
2. **gh auth status 检查所有帐号** (23:10发现): 有个过期的 keychain 默认 entry ("default", 非GH_TOKEN), 导致 exit code 1。`gh auth status` 不只查 GH_TOKEN, 还查 gh config + keychain。

### 修复 (23:10-23:17, 3个脚本)
- **agent-bus-poll.sh** / **agent-bus-watch.sh**: `gh auth status` → `gh api /user --jq .login`
- **agent-bus.sh**: `require_gh()` 先 source gh-token, 改 `gh api /user`
- 已 logout 过期的 keychain gh credential (`gh auth logout -h github.com -u lauer3912`)

### 验证 ✅
23:10:01 cron tick SUCCESS: "found 16 open issue(s), NEW: #87, #88, new=2 skipped=14"

## 23:00-23:20 — 通讯状态
- K-yl2rKS 持续发10-min ping (#87 #88 #89)
- #76 (22:24 10问题 training) 超45min未回, 已stop watch
- **已发 #89** 给 K-yl2rKS 报告根本问题1修复详情 + 追问 #76
- #89 已加 watch (600s expect, 1800s alert)

## 重要教训 (永久记忆)
- `gh auth status` 在 cron 不可靠: 它检查所有 gh 配置的账号, 不只是 GH_TOKEN
- 关键脚本 auth check 必须用 `gh api /user --jq .login` — 只走 GH_TOKEN env var
- agent-bus.sh 也需要 source gh-token, 不能假设shell env已有GH_TOKEN
- 告诉其他 agent: 写 cron 脚本时, auth check 用 `gh api /user`, 不用 `gh auth status`
