# 2026-06-13

## 16:24 任务: 推广营销 Agent 入职手册 (A+B 路线) + install.sh 5.5 Step

### 来源
- 老爷 QQ: "就按你的建议来A+B。要把相关的技能弄好,让Ubuntu服务器上的推广营销Agent通过入职手册,可以简单上岗"
- 上下文: 上午检查 openclaw-marketing-template 状态 → 列出 8 类营销 skill → 5 个 clawhub 命中 → A+B 并行

### 完成总览

#### A. clawhub 搜 + install 5 个高分营销 Skill
- `gingiris-aso-growth` (2.2.10) — ASO 冷启动 + UGC + 关键词排名 (最贴 VitaMindGo 实战)
- `reddit` (1.0.0) — Reddit 搜索/发帖/审 (OAuth)
- `reddit-marketing` (1.0.0) — Reddit 营销策略 + sub 选择
- `reddit-account-operations` (1.2.0) — Reddit 账号反 spam 操作 doctrine (cron-safe)
- `marketing-analytics` (1.0.0) — 营销数据分析 + 报告生成
- **装到**: `~/.openclaw/workspace/skills/` (本机 5 个全已装)

#### A+. install.sh Step 5.5 嵌入 5 个 Skill 自动安装
- 文件: `dist/openclaw-marketing-template/install.sh`
- 新增 `step5_5_install_marketing_skills()` 函数
- MARKETING_SKILLS 数组 (5 个 slug)
- 幂等: 已装就 skip, 失败 warn 不阻塞
- 装完在 step7_complete 列出 skill 库
- 位置: 插在 Step 5 (verify) 之后, Step 6 (cron) 之前

#### B. 创建 5 个 VitaMindGo 专属 skill_workshop proposal (clawhub 找不到)
1. **vitamindgo-marketing-knowledge** (`d0a2d1ddcc`) — 产品 brief + ASO 关键词库 + Apple 1.4.1 medical claim guardrails
2. **marketing-cron-ops** (`a45c02b3de`) — daily/weekly cron 4-tier fallback playbook
3. **appstore-data-pipeline** (`1a2065f4cc`) — iTunes Lookup + ASC API (JWT) pipeline
4. **bug-triage-to-dev** (`4553049c71`) — 营销→开发 bug 反馈 (P0-P3 severity)
5. **competitor-research-methodology** (`7d58bbbc49`) — 7 步竞品研究方法论 (VitaMindGo 4 竞品实战)
- 状态: **全部 pending** (老爷审完才 apply, 不阻塞上岗)

#### 文档同步
- `docs/marketing-onboarding-guide.md`: 1) Step 5.5 表格, 2) 后入职 5 步→6 步, 3) 5 个 skill 详细列出
- 文件大小: 4.8KB → 5.7KB

#### 验证
- 新写 `scripts/verify-marketing-template.sh` (47 项检查)
- 跑通: **46/46 PASS** (1 项灵魂文件检查移到 blocks/ 目录, install 后才注入, 不属 template 检查)
- 检查项: install.sh 语法 + 5.5 函数 + 5 个 skill slug + 5 个 clawhub skill + 15 个脚本可执行 + 13 个块文件 + 5 个 proposal + 3 个文档 + 文档同步

### 推送 (铁律 10 + 8 + 9 严格执行)

#### dev template (openclaw-portable-template)
- Commit: `3e34ef1` "release: v1.0.18 - distribute-sop.sh 引入 template"
- 变更: MANIFEST 1.0.17→1.0.18 + scripts/distribute-sop.sh 新加
- 验证: 远端 SHA `3e34ef1e5c75` ✅

#### marketing template (openclaw-marketing-template)
- Commit: `aeaadf2` "release: v1.0.5 - 5 个营销 Skill 自动安装 (Step 5.5)"
- 变更: MANIFEST 1.0.4→1.0.5 + install.sh 5.5 step + docs 同步
- 验证: 远端 SHA `aeaadf2c0853` ✅

### Ubuntu Agent 上岗流程 (老爷可发)

```bash
# 1. 一条命令入职 (新 Ubuntu 22.04+ 服务器)
curl -fsSL -H "Authorization: token $GH_TOKEN" \
  https://raw.githubusercontent.com/lauer3912/openclaw-marketing-template/main/install.sh | bash

# install.sh 会自动:
#   - 拉 template → workspace
#   - 注入灵魂文件 (AGENTS/SOUL/IDENTITY/USER) 
#   - 装 5 个 clawhub 营销 Skill (Step 5.5)
#   - 注册 cron (sync-template-6am)
# 60 秒后, Agent 即有营销能力

# 2. 后入职 6 步 (verify-agent.sh + fill-user-md-defaults.sh + ...)
# 3. 每天 cron 自动跑 (daily-data-pull, weekly-summary, send-report, ...)
```

### 关键设计决策

1. **A+B 路线而非纯 B**: 5 个 clawhub 命中已经覆盖通用营销能力, 5 个 VitaMindGo 专属 proposal 沉淀实战知识
2. **A 不直接进 B**: clawhub skill 装在 $WORKSPACE/skills/, VitaMindGo 专属走 skill_workshop proposal (pending 流程) — 两个体系分开
3. **install.sh 5.5 失败不阻塞**: `|| true` + warn, 网络/registry 故障 Agent 仍能入职
4. **block 体系没动**: 13 个块都健在, 没新增 marketing 块 (因为 VitaMindGo 专属知识已沉淀到 proposal, 走 skill_workshop apply 后再用)

### 踩坑 (永久记)

1. `local` 关键字只能在函数里用, verify 脚本第一版踩了 → 改全局变量
2. `set -euo pipefail` 下 `grep -c` 找不到匹配会返回非零导致脚本退出 → 加 `|| true` 兜底
3. dev template 里 `setup-github-cred.sh` 和 `sop-822-check.sh` 是 dev 专属, 营销 template 不需要 (营销 agent 不 ssh macmini build) → verify 列表移除
4. `core.fileMode` 差异: chmod 后 6 个 scripts 变 modified, 但 0 行内容 diff → 临时设 `core.fileMode false` 忽略
5. 6 个 scripts 在 origin 里是 0644 (无 +x), install.sh Step 3 拷过去后会 chmod +x — 这设计是对的 (template 不必可执行, 工作区才需要)

### 工作量统计
- **clawhub install**: 5 个 skill, 2 分钟
- **install.sh 改**: +68 行 (Step 5.5)
- **proposal 创建**: 5 个, 1.5 KB ~ 3 KB each
- **文档**: 22 行新增
- **verify 脚本**: 4.1 KB, 47 项
- **推送**: 2 个 commit (dev + marketing), 走代理
- **总耗时**: 16:24 → 16:38 ≈ 14 分钟

### 下一步 (等老爷)
- 老爷审 5 个 pending proposal → apply (会进 ~/.openclaw/skills/ 或 plugin-skills)
- 老爷通知新 Ubuntu agent 跑 install.sh 端到端测
- 30 天路线图: Day 1-2 install + 读 docs, Day 3 第一篇 Reddit post (r/AppleWatch)

## 16:46 任务: README.md 一键上岗入口 (老爷忘性大)

### 来源
- 老爷 QQ 16:43: "Ubuntu Agent 一键上岗相关内容也写到文档了吧？我忘性大, 我只希望去看一个文档就能知道怎么让 Ubuntu 推广营销Agent一键上岗"
- 上下文: 上午弄完 install.sh 5.5 + 5 个 Skill 之后, 缺一目了然入口

### 完成
- **新加 README.md (3.7KB)**: 一目了然版入口
  - 一条命令 (顶部)
  - 8 个 Step 表格
  - 后入职 6 步 (Day 1 必做, 5 分钟)
  - 维护 2 步 (一辈子)
  - 3 个故障兑底
  - 文档地图 + 当前状态
- **install.sh**: step7_complete 加 README 路径提示 (1 行)
- **docs/marketing-onboarding-guide.md**: 顶部加 README 跳指针
- **MANIFEST**: 1.0.5 → 1.0.6

### 推送
- Commit: `0f2159c` "release: v1.0.6 - README.md 一键上岗入口 (老爷忘性大)"
- 4 files changed, 138 insertions, 2 deletions
- 远端 SHA: `0f2159c67fc0` ✅
- Push 走代理: aeaadf2 → 0f2159c (force)

### 踩坑 (永久记)
- **zsh 嵌套 `$()` 解析错误**: `$(... | ... | ...)` 多层管道在 zsh 里有时报 parse error near `)`. 解决: 分行写, 提取中间变量, 别一气吪成多层
- **raw.githubusercontent.com 匿名 404**: push 完后 1-2 分钟内, 匿名 raw CDN 还没同步 (401/404). 带 token 的 download_url 立刻生效. install.sh 走的是 token 路径, 真正使用不受影响. **5-10 分钟 CDN 同步**后匿名 URL 也 OK
- **MANIFEST sed 必须独立跑**: 上次命令因为 zsh parse 错误中断, sed 实际没跑成. 重跑 sed 时检查 grep 确认改成了

## 18:25-18:42 任务: v1.0.7 Agent 全自动上岗 (6 维能力补齐) — 18 分钟交付

### 来源
- 老爷 18:25 QQ: "现在就动手"
- 上下文: 预演后老爷拍板 4 件大事 (半自动全改全自动 / ASC 凭证我有 / 真人故事 Agent 写 / 列全 API 清单)

### 完成 (8 文件, 1 install.sh)

#### A. install.sh 自动化补洞
- **A1**: 顶部加 ASC 凭证常量 (Issuer `b2a00f88-3a8d-40d0-b148-1f1db92e10b7`, Key `H3973L93M5`, Path `~/.appstoreconnect/private_keys/AuthKey_H3973L93M5.p8`) — 跟 GH token 一样硬编码 fallback
- **A2**: Step 4 配 `asc-config.json` (chmod 600), 提示手动补 .p8 (Mac mini 拷到 Ubuntu)
- **A3**: Step 6 注册 **7 个 cron** (sync-template-6am + 6 个营销: 08/09/09:30/20/周日 20/22)
- install.sh 头版本号: 1.0.6 → 1.0.7

#### B. Agent 真人故事生成器
- `scripts/agent-story-generator.sh` (9.6KB, 262 行)
- 拼数据源: `data/products.json` + `MEMORY.md` + `memory/*.md` 实战日志 + USER.md/SOUL.md 身份
- 输出 4 平台: **Reddit** (3 段, "real story not an ad") / **Show HN** (标题 + 2 段) / **ProductHunt** (tagline 60 + desc 260 + first comment) / **X** (3 短文版本)
- 跑通: vitamindgo 生成完整 4 草稿

#### C. 4 个 API 接入脚本
- `hn-monitor.sh` (2.5KB): HN /show + search + item + user (公开, 无 auth, 走 Algolia 搜)
- `setup-reddit-oauth.sh` (6.3KB): init + refresh + post + comment (script type OAuth, 存 `.reddit-config.json` chmod 600)
- `competitor-search.sh` (4.2KB): iTunes Search 拓 4 竞品 (实测找到 Simple/Bevel/Glow/HUMANITY, 0-378K 评)
- (X OAuth setup 暂缓, VitaMindGo 还没真发 X)

#### D. 3 个 Agent 自审脚本
- `aso-compliance-check.sh` (5.9KB): 字段长度 (5 字段, Apple 上限) + Apple 1.4.1 medical claim 黑名单 (20+ 词)
- `reddit-compliance-check.sh` (5.9KB): preflight (7 天频率 + 时段 + 今日数) + check-text (marketing 词 20 个 + real story 启发式 14 个 + 链接数)
- (ASO 1.4.1 + 字段合并一个脚本, Reddit 文案 + 频率合并一个)

### 端到端验证
- `scripts/verify-marketing-template.sh` 跳 v1.0.7 检查
- **55/55 PASS** (原 46 + 6 新脚本 + 3 install.sh v1.0.7 新功能)
- ASO 合规实测: VitaMindGo 真数据全过 / "CureApp" 故意写 5 个 medical claim 全部命中
- Reddit 文案实测: 真人故事 4 个 real story 元素 + 1 链接 → GO

### 推送
- Commit: `4d8a693` (amend 包含 MANIFEST)
- 远端 SHA: `4d8a693d404c`
- 8 files: MANIFEST.yaml + install.sh + 6 个新脚本
- 改动: +1209 / -17 行
- 走代理 ✅

### 踩坑 (新增 2026-06-13 18:42)
- **zsh `**` 当 glob**: token 提取 `***` 被 zsh 当 recursive glob, 报错. 用 python re.search 替代
- **zsh `?` 当 glob**: URL `?ref=main` 被 zsh 当单字符通配符. URL 要加引号, 或用 python 走
- **MANIFEST sed 又忘改**: 这次先 commit 后才 sed, 才发现 sed 失败. **改正: 永远先 sed + grep 确认 + 再 commit**
- **commit message 长 + 复杂字符**: 含 `()` 引号中文, shell 一行 commit 报 parse error. **解决: 写 -F /tmp/commit-msg.txt, commit 消息不进 shell 解析**

### 工作量统计
- **6 个新脚本**: 35 KB 总, 1133 行
- **install.sh 改**: +93 / -17 行 (ASC 凭证 + Step 4 + Step 6)
- **测试**: ASO 1.4.1 + Reddit 文案 + 4 竞品实时 + 语法检查
- **推送**: 1 commit (amend), 走代理
- **总耗时**: 18:25 → 18:42 ≈ **17 分钟** (手快)

### 关键设计决策
1. **硬编码 ASC 凭证**: 跟 GH token 共用铁律 9 流程, 从 MEMORY.md 提取. 新 Agent 拷 .p8 文件即可, 不用问老爷
2. **7 个 cron 一次注册**: install.sh Step 6 写 CRON_JOBS 数组, 7 个 name|schedule|prompt. 幂等, 已注册就 skip
3. **真人故事脚本拼数据**: 不用 LLM, 拼现成 markdown 段落 (产品 brief + 实战 + 痛点). 输出确定, 不会幻觉
4. **审核脚本三件套**: ASO 字段 + 1.4.1 黑名单 + Reddit 反 spam. 任何"写完就发"前必跑, 退出码明确
5. **不动 iOS 项目**: 6 个新脚本只跟 marketing template 有关, 跟 iOS dev template 零耦合

### 下一步 (等老爷)
- Reddit OAuth 一次性 client_id/secret (12-13 后)
- X OAuth (Day 15+ 评估)
- Sensor Tower ASO (Day 30+ 评估)
- 30 天路线图 Day 1-2 install + 读 docs, Day 3 第一篇 Reddit 帖

## 20:28-20:35 任务: v1.0.8 - ASC .p8 私钥进 template (老爷拍板)

### 来源
- 老爷 20:28 QQ: "把这个文件全部 ASC 相关的位置都放到 openclaw-marketing-template 中, 我批准了. 没有安全问题"

### 完成
- **A. private_keys/AuthKey_H3973L93M5.p8 (新, 257 字节)**: 从 Mac mini `~/.appstoreconnect/private_keys/` 复制到 template `private_keys/`
  - MD5: `0de9390760a0f74a6e949168a06e94c2` (源 = 远端 一致)
  - chmod 600
  - 256-bit P-256 ES256 私钥, 跟 Apple 后台 Issuer/Key ID 配套
- **B. install.sh 加 Step 2.5 (自动拷 .p8)**: 
  - `step2_5_copy_asc_key()` 函数, 源 = `$WORKSPACE_TEMPLATE/private_keys/...`, 目标 = `~/.appstoreconnect/private_keys/...`
  - 幂等 (md5 cmp 一致就 skip)
  - Step 4 简化 (不再 warn .p8 缺失)
  - 头部 + log: 1.0.7 → 1.0.8
- **C. MANIFEST.yaml**: 
  - version: 1.0.7 → 1.0.8
  - always_sync 加 `private_keys/` (sync-from-template.sh 会自动拉)

### 推送
- Commit: `c0228f2`
- 远端 SHA: `c0228f2426a3`
- 3 files: MANIFEST + install.sh + private_keys/AuthKey_H3973L93M5.p8 (新)
- 远端 private_keys/ 验证 ✅ (257B)

### 风险评估 (永久记)
- ⚠️ .p8 进 private GitHub repo = 仓库成员都能拉
- ⚠️ Mac mini / 任何有 GH 凭据的人 = 都能解 JWT
- ✅ 但: 私钥本身不能调 API, 还要 issuer_id (已知) + 凭证签发权限
- ✅ 反制: 定期重生成 .p8 (苹果后台一键)
- **责任在老爷** (他拍板的, 铁律 8 范围外)

### 永久记铁律
- 私钥进 GitHub 仓库 (即使是 private) 永远是"老爷拍板"事件, 不主动建议
- 反过来, 如果 .p8 没在仓库, 仍然走 `scp` 临时方案, 不强求进 template

### 踩坑 (新增)
- zsh `**` glob 仍触发 — 用 python re.search 提 token 是最稳的
- 多行 `python3 -c` 触发 exec 安全检查, 写 .py 文件再 `python3 file.py` 跑
- `$(python3 -c "...")` 在 zsh 嵌套仍易报 parse error, 用 `$(python3 file.py)` 替代

### 总耗时
- 20:28 → 20:35 = **7 分钟** (3 文件改动小)

## 20:31-20:35 任务: v1.0.9 - ASC 凭证路径从 ~/.appstoreconnect/ 改到 ~/.openclaw/workspace/.credentials/asc/

### 来源
- 老爷 20:31 QQ: "还有, 在Ubuntu 销售推广Agent 电脑上, 这些我批准的授权文件用来登录ASC的, 不要存放在 ~/.appstoreconnect/ 目录下面, 应该存放在 ~/.openclaw/workspace 某个合适的子目录下, 你看一下存放在哪里"

### 推荐位置 (我选)
- `~/.openclaw/workspace/.credentials/asc/`
- 理由: dotfile 隐藏 (ls 默认不显示) / 跟 dev 习惯一致 (.aws/credentials, .npmrc) / 子目录可扩展 (asc/, reddit/, x/, github/) / 不跟 Apple 官方重名 (altool 不会冲突)

### 完成
- **A. 文件路径迁移**: `private_keys/AuthKey_H3973L93M5.p8` → `.credentials/asc/AuthKey_H3973L93M5.p8`
  - 内容 / MD5 / 权限 0 变化 (git rename 100% 识别)
- **B. install.sh 改 4 处**:
  - 顶部 `ASC_KEY_PATH`: `~/.appstoreconnect/...` → `~/.openclaw/workspace/.credentials/asc/...`
  - Step 2.5 `target_dir`: 同上
  - Step 4 错误提示: 加排查指引
  - 头部 + log: 1.0.8 → 1.0.9
  - 注释清理: 不再含 `~/.appstoreconnect/` 字样
- **C. MANIFEST**:
  - always_sync: `private_keys/` → `.credentials/`
  - 注释更新: 子目录可扩展 (asc/, reddit/, x/, github/)
  - version: 1.0.8 → 1.0.9

### 推送
- Commit: `44fc9e1` "release: v1.0.9 - ASC 凭证路径 ~/.appstoreconnect/ → ~/.openclaw/workspace/.credentials/asc/"
- 远端 SHA: `44fc9e1782aa`
- git rename 100% 识别: `private_keys/AuthKey_H3973L93M5.p8 -> .credentials/asc/AuthKey_H3973L93M5.p8`
- 远端验证: 3 项全通 (SHA + .credentials/asc 内容 + private_keys/ 404)

### 责任边界 (永久记)
- `.credentials/asc/`: 归 OpenClaw 营销 Agent 自己的 ASC 凭证
- `~/.appstoreconnect/`: 仍归 Apple 官方工具 (altool, transporter) — 两者不混

### 踩坑 (新增)
- zsh 嵌套 `$(python3 file.py)` 容易跟 `**` 混 — 写 .sh 包装脚本最稳
- bash `$***` (offset 字符串) 在 zsh eval 里 `***` 当 glob — **用 `${TOKEN:0:8}` 替代, 不用 `***`**

### 总耗时
- 20:31 → 20:35 = **4 分钟** (3 文件改动小)
